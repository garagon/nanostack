interface:
  display_name: "nanostack-guard"
  short_description: "On-demand safety guardrails — blocks destructive operations, can keep writes within a chosen scope (guided), warns on risky commands."
  default_prompt: "Use nanostack-guard when working near production or sensitive systems."
policy:
  allow_implicit_invocation: true
